[project]
name = "agent-week1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jlefx", email = "jlee@serenetics.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.121.0",
]

[project.scripts]
agent-week1 = "agent_week1:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
